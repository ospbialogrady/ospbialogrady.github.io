document.addEventListener("DOMContentLoaded",()=>{const box=document.querySelector("#gallery-lightbox");if(!box)return;const imgs=[...document.querySelectorAll(".full-gallery-item img")],view=box.querySelector("img");let index=0;const show=i=>{index=(i+imgs.length)%imgs.length;view.src=imgs[index].src;view.alt=imgs[index].alt;box.classList.add("open");document.body.style.overflow="hidden"};const close=()=>{box.classList.remove("open");document.body.style.overflow=""};imgs.forEach((img,i)=>img.addEventListener("click",()=>show(i)));box.querySelector(".lightbox-close").addEventListener("click",close);box.querySelector(".lightbox-prev").addEventListener("click",()=>show(index-1));box.querySelector(".lightbox-next").addEventListener("click",()=>show(index+1));box.addEventListener("click",e=>{if(e.target===box)close()});document.addEventListener("keydown",e=>{if(!box.classList.contains("open"))return;if(e.key==="Escape")close();if(e.key==="ArrowLeft")show(index-1);if(e.key==="ArrowRight")show(index+1)})});